# Frontend values must use the VITE_ prefix.
VITE_API_BASE_URL=http://localhost:8000

# The POC keeps its SQLite configuration local. Story 0.2 will introduce it.
# CALL_RADAR_DATABASE_URL=sqlite:///./data/call_radar.db
